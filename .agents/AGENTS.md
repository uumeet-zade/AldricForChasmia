# Writing and Formatting Rules
- Never use "is not X - but Y" phrasing (or similar variations like "is not X, but Y").
- Never use em dashes (—). Always stick to regular hyphens (-).
- Stick strictly to the design template provided in the `design-template` directory when generating or modifying HTML/CSS. Do not invent new structures that deviate from it.
